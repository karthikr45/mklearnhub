apiVersion: v1
kind: Service
metadata:
  name: {{ include "learnhub.fullname" . }}-web
  labels:
    {{- include "learnhub.componentLabels" (dict "root" . "component" "web") | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.web }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learnhub.componentLabels" (dict "root" . "component" "web") | nindent 4 }}
